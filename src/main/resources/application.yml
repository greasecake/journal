spring:
  application:
    name: student_journal
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=student_journal
    username: postgres
    password: 1
server:
  port: 9000